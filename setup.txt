1.
    Docker =>
    Polaczenie przez datagrip:
        driver = Microsoft Sql Server,
        host = localhost,
        user = sa,
        password = yourStrong(!)Password
2.
    Database mssql, ale flask potrzebuje odbc 18:
        https://learn.microsoft.com/en-us/sql/connect/odbc/download-odbc-driver-for-sql-server?view=sql-server-ver16
4.
    po polaczeniu z baza
        terminal
        (jakby nie bylo migrations) flask db init
        (Migracje sa w migrations ale jakby nie bylo) flask db migrate -m "Initial migration1"
        flask db upgrade
        i main w seed data

5.
    postman: (Zamiast ręcznego wstawiania) // po przejsciu na cookies jest nie potrzebne
        sign in -> Envroiments -> new -> variables:
            base_url Init value = 127.0.0.1:5000, Current value = 127.0.0.1:5000
            token

        tworzysz request -> wstawiasz http://{{base_url}}/login -> Authorization -> type Bearer Token -> Token = {{token}}
        wtedy Tests -> wstawiasz "pm.test("Extract and save access_token to environment variable", function () {
    var responseBody = pm.response.json();
    var accessToken = responseBody.access_token;
    pm.environment.set('token', accessToken);
    console.log('Access token saved to environment variable:', accessToken);
});"



